version: 2

sources:
  - name: raw
    database: raw
    tables:
      - name: ad_events
        description: "Raw ad-serving events from the ad platform. ~131k rows over ~30 days."
      - name: publishers
        description: "Publisher dimension — 20 publishers."
      - name: campaigns
        description: "Advertiser campaign dimension"
      - name: ad_units
        description: "Ad unit placement configuration"
